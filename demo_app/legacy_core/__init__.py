"""LegacyCore — a synthetic credit-union employee workstation used as the automation target.

This is the app-under-test, deliberately kept separate from the ``computer_use`` package:
it must not import the contract layer. Records are synthetic and contain no real PII.
"""
